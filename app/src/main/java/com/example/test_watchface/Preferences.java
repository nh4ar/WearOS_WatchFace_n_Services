package com.example.test_watchface;

import android.os.Environment;

public class Preferences {

    public String Directory = Environment.getExternalStorageDirectory() + "/DataCentric/";        // Directory on the watch where all files are saved

}
